# SaaS Workspaces API

Backend-only SaaS built with FastAPI, Python 3.12, and PostgreSQL.

## Quick start

```bash
poetry install
poetry shell
uvicorn app.main:app --reload
```
Then visit http://localhost:8000/health.
